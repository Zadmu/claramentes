import React from 'react';
import Header from './components/Header';

const Groups = () => {
    return (
        <div>
            <div className = "Header">
                <Header />
            </div>
            <h1>
                Browse local internships, community services, and classes!
            </h1>
        </div>
    );
}

export default Groups;